{
  "lexicon": 1,
  "id": "is.dame.creating.ratioed.template",
  "defs": {
    "main": {
      "type": "record",
      "description": "The standing text a Ratioed piece is written from, kept as a record so it can be edited without a deploy. A singleton at rkey \"self\". The wording has drifted across the series — take #13 dropped the word \"experimental\" that takes #1 to #12 all carried, and that drift is the whole reason this is editable rather than compiled in. The discovery scan reads this record as well, and matches posts on the template's own lines, so a rewording needs no code change on either side. What is still checked before a save is the take line and the link: if the template is rewritten again before a piece written from the old one has been scanned, those two are all that is left to find it by, and a piece the site never finds is a piece it never measures.",
      "key": "literal:self",
      "record": {
        "type": "object",
        "required": ["text"],
        "properties": {
          "text": {
            "type": "string",
            "maxLength": 3000,
            "description": "The post body. Two placeholders are substituted when a piece is composed: {take} becomes the take number, and {link} becomes the URL of that piece's own page on the site. Everything else is written verbatim."
          },
          "announcement": {
            "type": "string",
            "maxLength": 3000,
            "description": "The whole concluding reply. Substituted when a piece is sealed: {handle} is the account whose like ended it, {take} the take number, {engagement} what it had drawn, {duration} how long it stood, {counts} the same list without the \"and\", {rank} the standing that earned when it earned one, {participant} the breaker's page on the site and {link} the piece's own. Links are stored the way a client stores them: the shortened form in the text and the whole URL in a facet beside it, which is what a 300-character post is counted against. A line of \"---\" on its own starts a second reply in the same thread, for a reply that outgrows one post. The phrase \"was to blame\" is load-bearing: it is what the site parses the breaker back out of, and for a piece whose like was later deleted that reply is the only evidence the like ever existed."
          },
          "updatedAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
