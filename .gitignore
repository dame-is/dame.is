node_modules
.DS_Store
dist/
.vite/
public/data/
# Generated by scripts/prefetch.mjs at build time (public/robots.txt is static).
public/sitemap.xml
public/feed.xml
public/llms.txt
*.local
.env
.env.local
.vercel
# iCloud occasionally duplicates files with " 2." suffix during sync — never commit those.
* 2.js
* 2.jsx
* 2.json
* 2.css
* 2.md
* 2.html
* 2.mjs

# Editor swap files (nano leaves ..file.swp next to the file being edited).
*.swp

# Runtime state for services/mod-consumer — a resume point and an answered set,
# written next to the code because that is the service working directory.
services/mod-consumer/mod-consumer-cursor.txt
services/mod-consumer/mod-consumer-answered.json
